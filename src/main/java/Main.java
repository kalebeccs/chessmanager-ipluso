package src.main.java;

import src.main.java.application.ChessApplication;

/**
 * The Main class serves as the entry point for the Chess application.
 * It initializes and starts the ChessApplication.
 */
public class Main {
    /**
     * The main method is the entry point of the application.
     * It creates an instance of ChessApplication to start the application.
     *
     * @param args Command-line arguments (not used).
     */
    public static void main(String[] args) {
        new ChessApplication();
    }
}